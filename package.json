{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run build:frontend && pnpm run build:api",
    "build:frontend": "BASE_PATH=${BASE_PATH:-/} pnpm --filter @workspace/prop-firm run build",
    "build:api": "pnpm --filter @workspace/api-server run build",
    "start": "NODE_ENV=production pnpm --filter @workspace/api-server run start",
    "dev": "pnpm --filter @workspace/api-server run dev",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
