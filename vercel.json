{
  "version": 2,
  "installCommand": "corepack enable && pnpm install --frozen-lockfile=false",
  "buildCommand": "BASE_PATH=/ pnpm --filter @workspace/prop-firm run build",
  "outputDirectory": "artifacts/prop-firm/dist/public",
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
